#include <iostream>

using namespace std;

int main() {
    int n;
    cin >> n;

    bool found = false;

    for (int i = 0; i < n; ++i) {
        if () {
            found = true;
        }
    }

    if (!found) {
        cout << -1;
    }

    cout << endl;
}